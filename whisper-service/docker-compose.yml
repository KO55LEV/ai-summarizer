services:
  whisper:
    build:
      context: .
    container_name: whisper-service
    restart: unless-stopped
    env_file:
      - ../.env
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - whisper-models:/models
      - whisper-temp:/tmp

volumes:
  whisper-models:
  whisper-temp:

insert into prompt_runs (
    id,
    prompt_id,
    workflow_id,
    step_key,
    prompt_key,
    title,
    workflow_type,
    provider,
    model,
    request_json,
    response_json,
    status,
    error_code,
    error_message,
    input_tokens,
    output_tokens,
    total_tokens,
    duration_ms,
    started_at,
    finished_at,
    created_at,
    updated_at
)
values (
    @id,
    @prompt_id,
    @workflow_id,
    @step_key,
    @prompt_key,
    @title,
    @workflow_type,
    @provider,
    @model,
    @request_json,
    @response_json,
    @status,
    @error_code,
    @error_message,
    @input_tokens,
    @output_tokens,
    @total_tokens,
    @duration_ms,
    @started_at,
    @finished_at,
    @created_at,
    @updated_at
)
returning id, prompt_id, workflow_id, step_key, prompt_key, title, workflow_type, provider, model, request_json::text as request_json, response_json::text as response_json, status, error_code, error_message, input_tokens, output_tokens, total_tokens, duration_ms, started_at, finished_at, created_at, updated_at;
